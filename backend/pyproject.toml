[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "employability-backend"
version = "0.1.0"
description = "FastAPI backend (not deployed via Vercel)"
# No Vercel entrypoint defined - this prevents framework detection
